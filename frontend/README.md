# 언니의 소개 - 프리미엄 매칭 서비스 프로토타입

> **모바일 우선의 컬러풀한 데이팅 앱 프로토타입**  
> Next.js + TypeScript + Tailwind CSS + Zustand로 구현된 DB 없는 프로토타입

## 🌈 프로젝트 특징

- **모바일 최적화**: 375px 기준 모바일 우선 디자인
- **컬러풀 UI/UX**: 핑크/보라/민트 그라디언트와 둥근 모서리
- **완전한 시뮬레이션**: 실제 로그인/결제 없이 모든 플로우 체험 가능
- **반응형 디자인**: 모바일, 태블릿, 데스크탑 대응
- **애니메이션**: Framer Motion으로 부드러운 인터랙션

## 🚀 실행 방법

### 1. 의존성 설치
```bash
npm install
```

### 2. 개발 서버 실행
```bash
npm run dev
```

### 3. 브라우저에서 확인
```
http://localhost:3000
```

### 4. 프로덕션 빌드 (선택사항)
```bash
npm run build
npm start
```

## 📱 주요 페이지 및 기능

### 🏠 메인 페이지 (`/`)
- Hero 섹션 (그라디언트 배경, 애니메이션 blob)
- 서비스 특징 소개
- 이용 절차 (7단계)
- 요금제 소개
- CTA 섹션

### 👤 회원가입 (`/signup/*`)
- **Step 1** (`/signup/step1`): 기본정보 + 본인인증 시뮬레이션
- **Step 2** (`/signup/step2`): 프로필 정보 (성별, 직업, 취미, 성격, 이상형)
- **Step 3** (`/signup/step3`): 사진/서류 업로드 시뮬레이션
- **완료** (`/signup/done`): 가입 완료 페이지

### 📝 설문 조사 (`/survey/[matchingId]`)
- 객관식 4문항 (만족도, 추천의향, 서비스품질, 편의성)
- 주관식 1문항 (자유 피드백)
- 시각적 선택 UI (이모지 + 색상)

### 💳 결제 시스템 (`/payment/*`)
- **요금제** (`/payment/plans`): 3종 멤버십 카드 (여성/남성/Black)
- **결제** (`/payment/checkout`): 결제 방법 선택 + 약관 동의

### ⚙️ 설정 (`/settings`)
- 프로필 요약 카드
- 언어 토글 (한국어/English)
- 알림 설정 (푸시/이메일/SMS)
- 테마 설정 (라이트/다크)
- 고객지원 메뉴

### 👥 프로필 (`/profile`)
- 프로필 완성도
- 매칭 통계 (좋아요, 성공, 대화 중)
- 프로필 편집 버튼

## 🏗️ 폴더 구조

```
├── pages/                    # Next.js 페이지 (Pages Router)
│   ├── index.tsx            # 메인 페이지
│   ├── signup/              # 회원가입 플로우
│   │   ├── step1.tsx        # 기본정보 입력
│   │   ├── step2.tsx        # 프로필 정보
│   │   ├── step3.tsx        # 사진/서류 업로드
│   │   └── done.tsx         # 가입 완료
│   ├── survey/
│   │   └── [matchingId].tsx # 동적 설문 페이지
│   ├── payment/
│   │   ├── plans.tsx        # 요금제 선택
│   │   └── checkout.tsx     # 결제 시뮬레이션
│   ├── settings/
│   │   └── index.tsx        # 설정 페이지
│   ├── profile.tsx          # 프로필 페이지
│   ├── api/                 # Mock API 라우트
│   │   ├── members.ts       # 회원 정보 API
│   │   ├── survey.ts        # 설문 제출 API
│   │   ├── payments.ts      # 요금제/결제 API
│   │   └── settings.ts      # 설정 API
│   ├── _app.tsx            # 앱 진입점
│   └── _document.tsx       # HTML 문서 구조
├── components/               # 재사용 컴포넌트
│   ├── PageHeader.tsx       # 상단 헤더 (뒤로가기, 언어토글)
│   ├── BottomNav.tsx        # 하단 탭 네비게이션
│   ├── StepForm.tsx         # 다단계 폼 레이아웃
│   ├── PlanCard.tsx         # 요금제 카드
│   ├── ProfileUploader.tsx  # 파일 업로드 컴포넌트
│   └── Toast.tsx           # 토스트 알림
├── stores/                  # Zustand 상태 관리
│   ├── useAuthStore.ts      # 인증 상태
│   ├── useSignupStore.ts    # 회원가입 상태
│   └── useSettingsStore.ts  # 설정 상태
├── hooks/                   # 커스텀 훅
│   └── useToast.ts         # 토스트 알림 훅
├── styles/
│   └── globals.css         # 전역 스타일 + 커스텀 애니메이션
├── public/                 # 정적 파일
└── mocks/                  # 더미 데이터 (기존)
```

## 🎨 디자인 시스템

### 컬러 팔레트
```css
/* Primary Colors */
--pink: #FF5C8A → #F472B6     /* 여성 회원용 */
--mint: #00D1B2 → #10B981     /* 남성 회원용 */  
--purple: #6C5CE7 → #8B5CF6   /* 프리미엄/Black 회원용 */

/* Gradients */
from-pink-500 to-rose-500     /* 여성 테마 */
from-emerald-500 to-teal-600  /* 남성 테마 */
from-violet-500 to-purple-600 /* 프리미엄 테마 */
```

### 컴포넌트 스타일
```css
/* Cards */
.card { @apply rounded-2xl bg-white shadow-sm border border-gray-200 p-6; }

/* Buttons */
.btn-primary { @apply h-12 px-6 rounded-full bg-gradient-to-r from-violet-500 to-purple-600 text-white font-semibold; }
.btn-secondary { @apply h-12 px-6 rounded-full border border-gray-300 text-gray-700; }

/* Form Controls */
.input { @apply h-12 px-4 rounded-xl border-2 border-gray-200 focus:border-violet-500; }
```

## 📊 더미 데이터 구조

### 회원 정보 (`/api/members`)
```typescript
interface Member {
  id: string
  name: string
  age: number
  gender: 'male' | 'female'
  occupation: string
  education: string
  location: string
  profileImage: string
  membershipType: 'female' | 'male' | 'black'
  isOnline: boolean
  lastSeen: string
}
```

### 설문 응답 (`/api/survey`)
```typescript
interface SurveySubmission {
  matchingId: string
  userId: string
  answers: Array<{
    questionId: string
    answer: string
    type: 'radio' | 'text'
  }>
  submittedAt: string
}
```

### 요금제 (`/api/payments`)
```typescript
interface PaymentPlan {
  id: 'female' | 'male' | 'black'
  name: string
  price: string
  period: string
  color: 'pink' | 'mint' | 'purple'
  features: Array<{
    text: string
    included: boolean
  }>
  isPopular?: boolean
  isPremium?: boolean
}
```

## 🔧 기술 스택

### Core
- **Next.js 15.5.2** (Pages Router)
- **TypeScript 5.6.2**
- **React 18.3.1**

### Styling & UI
- **Tailwind CSS 3.4.16**
- **Pretendard Variable** (한글 최적화 웹폰트)
- **Framer Motion 12.23.12** (애니메이션)
- **Lucide React 0.542.0** (아이콘)

### State Management & Forms
- **Zustand 5.0.2** (전역 상태)
- **React Hook Form 7.54.0** (폼 관리)
- **Zod 3.24.1** (스키마 검증)

### Development
- **ESLint** (코드 품질)
- **PostCSS & Autoprefixer** (CSS 처리)

## 📱 모바일 최적화 요소

### 터치 인터랙션
- **Touch Targets**: 최소 44px 크기 보장
- **Hover States**: 모바일에서 적절히 처리
- **Swipe Gestures**: 캐러셀에서 지원

### 성능 최적화
- **Lazy Loading**: 이미지 및 컴포넌트
- **Bundle Splitting**: 페이지별 코드 분할
- **SSR**: 초기 로딩 속도 향상
- **Pretendard Variable**: 한글 최적화 가변 폰트

### 반응형 디자인
```css
/* Breakpoints */
sm: '640px'   /* 태블릿 */
md: '768px'   /* 작은 데스크탑 */
lg: '1024px'  /* 데스크탑 */
xl: '1280px'  /* 큰 데스크탑 */
```

## 🎯 주요 완료 기능

### ✅ 완료된 기능
- [x] 모바일 우선 반응형 디자인
- [x] 회원가입 3단계 플로우
- [x] 설문 시스템 (객관식 4 + 주관식 1)
- [x] 요금제 및 결제 시뮬레이션
- [x] 설정 페이지 (언어, 알림, 테마)
- [x] 상태 관리 (Zustand)
- [x] 폼 검증 (React Hook Form + Zod)
- [x] 애니메이션 효과 (Framer Motion)
- [x] 토스트 알림 시스템
- [x] Mock API 라우트

### 🔄 미완성 기능 (추후 확장 가능)
- [ ] 다국어 지원 (next-intl)
- [ ] 실제 매칭 로직
- [ ] 채팅 시스템
- [ ] 실시간 알림
- [ ] 이미지 최적화
- [ ] PWA 기능

## 🎮 테스트 시나리오

### 1. 회원가입 플로우
1. 메인 페이지에서 "시작하기" 클릭
2. Step 1: 이름, 전화번호 입력 → "휴대폰 본인인증" 클릭 (2초 후 완료)
3. Step 2: 성별, 직업, 취미 등 선택
4. Step 3: 사진/서류 드래그 앤 드롭 업로드
5. 완료 페이지에서 "멤버십 플랜 보기" 클릭

### 2. 설문 시스템
1. URL에 `/survey/match123` 직접 입력
2. 4개 객관식 문항 선택 (이모지 버튼)
3. 주관식 의견 작성 (최소 10자)
4. "피드백 제출하기" → 성공 토스트 확인

### 3. 결제 시뮬레이션
1. `/payment/plans`에서 원하는 플랜 선택
2. 결제 방법 선택 (카드/휴대폰/무통장입금)
3. 약관 동의 체크
4. "결제하기" → 2초 로딩 후 성공 메시지

### 4. 설정 테스트
1. 하단 탭에서 "설정" 클릭
2. 언어 토글 (한국어 ↔ English)
3. 알림 토글 (푸시/이메일/SMS)
4. 테마 토글 (라이트 ↔ 다크)

## 📧 문의 및 지원

프로젝트 관련 문의사항이 있으시면 언제든 연락주세요.

---

**© 2024 언니의 소개. All rights reserved.**